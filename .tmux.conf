# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind C-a send-prefix

# split panes using C-a \ and C-a -
bind \ split-window -h -c '#{pane_current_path}'
bind - split-window -v -c '#{pane_current_path}'
unbind '"'
unbind %

# pane movement
#bind-key j command-prompt -p "join pane from:"  "join-pane -s '%%'"
bind-key @ command-prompt -p "send pane to:"  "join-pane -h -t '%%'"

# escape time
set-option -sg escape-time 10

# True color
set-option -g default-terminal "screen-256color"

# vi-mode and pane switching
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# reload tmux conf file
bind r respawn-pane -k
bind-key R source-file /home/seth/.tmux.conf \; display-message "~/.tmux.conf reloaded"
#bind-key R source-file ~/.tmux.conf

set-option -g mouse on

# tmux line snapshot
if-shell "test -f /home/seth/.tmux-snapshot" "source /home/seth/.tmux-snapshot"

# Increase scrollable history
set-option -g history-limit 5000
